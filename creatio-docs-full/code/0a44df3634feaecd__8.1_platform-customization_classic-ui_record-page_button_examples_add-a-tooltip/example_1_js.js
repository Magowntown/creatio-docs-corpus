define("ContactPageV2", [], function () {    return {        /* The name of the record page entity schema. */        entitySchemaName: "Contact",        /* The tooltip view. */        diff: /**SCHEMA_DIFF*/[            /* Properties to add the tooltip to the button. */            {                /* Run the operation that modifies the existing element. */                "operation": "merge",                /* The meta name of the parent container to add the button. */                "parentName": "LeftContainer",                /* Add the button to the element collection of the parent element. */                "propertyName": "items",                /* The meta name of the changed button. */                "name": "SaveButton",                /* The properties to pass to the element’s constructor. */                "values": {                    /* The tooltip array for the button. */                    "tips": []                }            },            /* The configuration object for a simple tooltip. */            {                /* Run the operation that inserts the element. */                "operation": "insert",                /* The meta name of the added button. */                "parentName": "SaveButton",                /* Add the tooltip to the element collection of the parent element. */                "propertyName": "tips",                /* The meta name of the added tooltip. */                "name": "CustomShowedTip",                /* The properties to pass to the element’s constructor. */                "values": {                    /* The tooltip text. */                    "content": {"bindTo": "Resources.Strings.SaveButtonHint"}                    /* Here, you can additionally configure other parameters for the tooltip view and logic. */                }            },        ]/**SCHEMA_DIFF*/    };});