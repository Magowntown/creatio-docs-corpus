/* Create an EntitySchemaQuery class instance with the Contact root schema. */var esq = this.Ext.create("Terrasoft.EntitySchemaQuery", {    rootSchemaName: "Contact"});/* Join another Contact schema to the root schema by the Owner column and select the Name column from the schema. Set its alias to OwnerName. */esq.addColumn("[Contact:Id:Owner].Name", "OwnerName");/* Join the Contact schema to the Account lookup column by the PrimaryContact column and select the Name column from the schema. Set the alias of the Name column to PrimaryContactName. */esq.addColumn("Account.[Contact:Id:PrimaryContact].Name", "PrimaryContactName");