define("ContactPageV2", [], function () {    return {        /* The name of the record page object's schema. */        entitySchemaName: "Contact",        /* Display the tooltip. */        diff: /**SCHEMA_DIFF*/[            /* The properties to add the tooltip to the button. */            {                /* Execute the operation that modifies the existing element. */                "operation": "merge",                /* The meta name of the field to change. */                "name": "Type",                /* The meta name of the parent container whose field to change. */                "parentName": "ContactGeneralInfoBlock",                /* Change the field in the element collection of the parent element. */                "propertyName": "items",                /* The properties to pass to the element's constructor. */                "values": {                    /* The property of the field that displays the tooltip. */                    "tip": {                        /* The tooltip text. */                        "content": { "bindTo": "Resources.Strings.TypeFieldHint" },                        /* The display mode of the tooltip.                        By default, WIDE. Changes the width of the green line displayed in the tooltip. */                        "displayMode": Terrasoft.controls.TipEnums.displayMode.WIDE                    }                }            }        ]/**SCHEMA_DIFF*/    };});