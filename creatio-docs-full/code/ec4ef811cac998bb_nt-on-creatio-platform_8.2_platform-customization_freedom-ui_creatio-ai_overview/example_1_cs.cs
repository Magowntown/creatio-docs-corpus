// IMPORTANT: When implementing// long-running operations,// it is crucial to enable timely and// responsive cancellation. To achieve// this, ensure that your code// is designed to respond appropriately// to cancellation requests using// the context.CancellationToken// mechanism. For more detailed// information and examples,// please, refer to our documentation./* Retrieve the input parameter of the AI Skill. */var someVariable = Get<string>("SomeInputParameter");/* Create a "CopilotIntentCall" instance. */var request = new CopilotIntentCall {    /* Code of the AI Skill to run. */    IntentName = "CodeOfSomeAISkill",    /* Parameters of the AI Skill. */    Parameters = new Dictionary<string, object> {        { "SomeInputParameter", someVariable }    }};/* Create an instance of the "CopilotIntentCall" object. */CopilotIntentCallResult result = UserConnection.CopilotEngine.ExecuteIntent(request);/* Status of the Creatio.ai execution. */Set("ParameterThatStoresExecutionStatus", result.Status.ToString());if (!result.IsSuccess) {    /* If Creatio cannot run the AI Skill, implement custom business logic that handles an error. */    ...    return true;}/* Outcome of the Creatio.ai work. */Set("ParameterThatStoresCreatioAIResult", result.Content);return true;