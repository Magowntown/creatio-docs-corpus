handlers: /**SCHEMA_HANDLERS*/[    {        request: "usr.SomeCustomRequest",        /* The implementation of the custom request handler. */        handler: async (request, next) => {            /* Retrieve the instance of the HTTP client from "@creatio-devkit/common." */            const handlerChain = sdk.HandlerChainService.instance;            /* Send the base request handler to open the page of the "SomeEntityCode" schema of "Object" type using the specified ID. */            await handlerChain.process({                type: 'crt.UpdateRecordRequest',                entityName: 'SomeSchemaName',                recordId: 'Some-Record-Id',                $context: request.$context            });            /* Call the next handler if it exists and return its result. */            return next?.handle(request);        }    }] /**SCHEMA_HANDLERS*/,