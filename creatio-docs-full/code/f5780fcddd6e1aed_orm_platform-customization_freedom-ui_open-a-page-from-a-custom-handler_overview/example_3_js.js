handlers: /**SCHEMA_HANDLERS*/[    {        request: "usr.SomeCustomRequest",        /* The implementation of the custom request handler. */        handler: async (request, next) => {            /* Retrieve the instance of the HTTP client from "@creatio-devkit/common." */            const handlerChain = sdk.HandlerChainService.instance;            /* Send the base request handler to open the new page of the "SomeEntityCode" schema of "Object" type. Populate the "Some field" field using the "Some value" value. */            await handlerChain.process({                type: 'crt.CreateRecordRequest',                entityName: 'SomeEntityCode',                    defaultValues: [{                        attributeName: 'SomeField',                        value: 'Some value'                    }],                    $context: request.$context            });            /* Call the next handler if it exists and return its result. */            return next?.handle(request);        }    }] /**SCHEMA_HANDLERS*/,