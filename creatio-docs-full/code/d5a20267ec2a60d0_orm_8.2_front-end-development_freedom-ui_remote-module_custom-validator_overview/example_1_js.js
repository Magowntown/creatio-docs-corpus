/* Import the required functionality from the libraries. */...import { InjectionToken } from "@angular/core";/* The SOME_TOKEN_NAME token that works with Angular DI (dependency injection). */export const SOME_TOKEN_NAME = new InjectionToken<string>('SOME_TOKEN_NAME');...export class SomeValidatorNameValidator extends BaseValidator {    /* Mark DI in the constructor using the @CrtInject decorator. */    constructor(@CrtInject(SOME_TOKEN_NAME) private _name: string) {        super();    }    ...}