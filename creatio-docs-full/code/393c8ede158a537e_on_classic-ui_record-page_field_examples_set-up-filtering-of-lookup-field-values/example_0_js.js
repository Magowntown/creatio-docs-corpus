define("AccountPageV2", [], function() {    return {        /* The name of the record page object's schema. */        "entitySchemaName": "Account",        /* The attributes of the view model. */        "attributes": {            /* The name of the view model column. */            "Owner": {                /* The data type of the view model column. */                "dataValueType": Terrasoft.DataValueType.LOOKUP,                /* The configuration object of the LOOKUP type attribute. */                "lookupListConfig": {                    /* The array of filters to apply to the query that populates the lookup field with data. */                    "filters": [                        function() {                            var filterGroup = Ext.create("Terrasoft.FilterGroup");                            /* Add the "IsUser" filter to the resulting filter collection.                            Select all records with non-null [Id] from the [Contact] root schema to which the [Id] column from the [SysAdminUnit] schema is joined. */                            filterGroup.add("IsUser", Terrasoft.createColumnIsNotNullFilter("[SysAdminUnit:Contact].Id"));                            /* Add the "IsActive" filter to the resulting filter collection.                            Select all records for which Active=true from the [Contact] root schema to which the [Active] column from the [SysAdminUnit] schema is joined. */                            filterGroup.add("IsActive",                                Terrasoft.createColumnFilterWithParameter(                                    Terrasoft.ComparisonType.EQUAL,                                    "[SysAdminUnit:Contact].Active",                                    true));                            return filterGroup;                        }                    ]                }            }        }    };});