define("AccountPageV2", [], function() {    return {        /* The name of the record page entity schema. */        entitySchemaName: "Account",        /* The methods of the record page view model. */        methods: {            /* Check whether the [Primary contact] field of the record page is filled. */            isAccountPrimaryContactSet: function() {                return this.get("PrimaryContact") ? true : false;            },            /* The button click handler method. */            onOpenPrimaryContactClick: function() {                var primaryContactObject = this.get("PrimaryContact");                if (primaryContactObject) {                    /* Get the ID of the primary contact. */                    var primaryContactId = primaryContactObject.value;                    /* Create an address string. */                    var requestUrl = "CardModuleV2/ContactPageV2/edit/" + primaryContactId;                    // Publish a message about updating the page navigation history and go to the primary contact page. */                    this.sandbox.publish("PushHistoryState", {                        hash: requestUrl                    });                }            }        },        /* Display the button on the record page. */        diff: [            /* Properties to add the custom button to the page. */            {                /*  Run the operation that inserts the element to the page. */                "operation": "insert",                /* The meta name of the parent container to add the button. */                "parentName": "LeftContainer",                /* Add the button to the element collection of the parent element. */                "propertyName": "items",                /* The meta name of the added button. */                "name": "PrimaryContactButton",                /* The properties to pass to the element’s constructor. */                "values": {                    /* Set the type of the added element to "button." */                    "itemType": Terrasoft.ViewItemType.BUTTON,                    /* Bind the button title to the localizable schema string. */                    "caption": {bindTo: "Resources.Strings.OpenPrimaryContactButtonCaption"},                    /* Bind the button click handler method. */                    "click": {bindTo: "onOpenPrimaryContactClick"},                    /* Bind the button availability property. */                    "enabled": {bindTo: "isAccountPrimaryContactSet"},                    /* The display style of the button. */                    "style": Terrasoft.controls.ButtonEnums.style.BLUE                }            }        ]    };});