define("ContactPageV2", [], function() {    return {        /* The name of the record page object's schema. */        entitySchemaName: "Contact",        /* Display the field on the record page. */        diff: [            /* Properties to add a [Country] field to the page. */            {                /* Add the element to the page. */                "operation": "insert",                /* The meta name of the parent container to add the field. */                "parentName": "ProfileContainer",                /* Add the field to the element collection of the parent element. */                "propertyName": "items",                /* The meta name of the field to add. */                "name": "Country",                /* The properties to pass to the element's constructor. */                "values": {                    /* Set the field type to lookup. */                    "contentType": Terrasoft.ContentType.LOOKUP,                    /* Set up the field layout. */                    "layout": {                        /* The column number. */                        "column": 0,                        /* The row number. */                        "row": 6,                        /* The column span. */                        "colSpan": 24                    }                }            }        ]    };});