/* Define the record page schema and its dependencies. */define("ContactPageV2", ["BaseFiltersGenerateModule", "BusinessRuleModule", "ContactPageV2Resources", "ConfigurationConstants", "ContactCareer", "DuplicatesSearchUtilitiesV2", "UsrAccountProfileSchema"], function (BaseFiltersGenerateModule, BusinessRuleModule, resources, ConfigurationConstants, ContactCareer) {    return {        entitySchemaName: "Contact",        /* The modules. */        modules: /**SCHEMA_MODULES*/{            /* The account profile module. */            "AccountProfile1": {                /* The profile configuration. */                "config": {                    /* The schema name. */                    "schemaName": "UsrAccountProfileSchema",                    /* The flag that communicates the initialization of the schema configuration. */                    "isSchemaConfigInitialized": true,                    /* The flag that specifies that HistoryState is not used. */                    "useHistoryState": false,                    /* The profile parameters. */                    "parameters": {                            /* The view model configuration. */                        "viewModelConfigDiff": {                            /* The name of the connected entity column. */                            masterColumnName: "Account"                        }                    }                }            }        }/**SCHEMA_MODULES*/,        /* The array of modifications. */        diff: /**SCHEMA_DIFF*/[            {                /* The insert operation. */                "operation": "insert",                /* The name of the parent element to insert the element. */                "parentName": "LeftModulesContainer",                /* The name of the parent element property on which to operate. */                "propertyName": "items",                /* The entity name. */                "name": "AccountProfile1",                /* The value of the element to add. */                "values": {                    /* Set the element type to module. */                     "itemType": Terrasoft.ViewItemType.MODULE                }            }        ]/**SCHEMA_DIFF*/    };});