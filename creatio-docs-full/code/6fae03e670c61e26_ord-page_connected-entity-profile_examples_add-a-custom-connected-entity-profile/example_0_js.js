/* Specify the ProfileSchemaMixin mixin as a dependency. */define("UsrAccountProfileSchema", ["ProfileSchemaMixin"], function () {    return {        /* The name of the object schema. */        entitySchemaName: "Account",        /* Mixins. */        mixins: {            /* The mixin with functions that retrieve profile icons and images. */            ProfileSchemaMixin: "Terrasoft.ProfileSchemaMixin"        },        /* The diff array of modifications. */        diff: /**SCHEMA_DIFF*/[            {                /* The insert operation. */                "operation": "insert",                /* The entity name. */                "name": "Contact",                /* The name of the parent element to insert the element. */                "parentName": "ProfileContentContainer",                /* The parent element property on which to operate. */                "propertyName": "items",                /* The element value. */                "values": {                    /* Bind the Contact object to the value of the Account property. */                    "bindTo": "Account",                    /* The layout configuration. Element position. */                    "layout": {                        "column": 3,                        "row": 10,                        "colSpan": 19                    }                }            }            /* Other configuration objects of the array of modifications. */        ]/**SCHEMA_DIFF*/    };});