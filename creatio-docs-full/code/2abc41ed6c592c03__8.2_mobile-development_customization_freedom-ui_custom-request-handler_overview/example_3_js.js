/* Import the required functionality from the libraries. */import {    CrtModule,    bootstrapCrtModule,    DoBootstrap} from '@creatio/mobile-common';...@CrtModule({    ...,    /* Specify that "SomeRequestNameHandler" is request handler. */    requestHandlers: [        SomeRequestNameHandler    ],})export class SomeMainAppClass implements DoBootstrap {    bootstrap(): void {        bootstrapCrtModule('SomeMainAppClass', SomeMainAppClass);    }}