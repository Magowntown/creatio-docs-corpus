define("ProjectPageV2", [], function() {    return {        /* The name of the record page object's schema. */        entitySchemaName: "Project",        /* The methods of the record page's view model. */        methods: {            /* Overload the base Terrasoft.BasePageV2.onEntityInitialized method that is called after Creatio initializes the schema of the record page object. */            onEntityInitialized: function() {                /* Call the parent implementation of the method. */                this.callParent(arguments);                /* Call the handler method that calculates the [Deadline] column value. */                this.setDeadline();            },            /* The handler method that calculates the [Deadline] column value. */            setDeadline: function() {                /* The [Deadline] column value. */                var deadline = this.get("Deadline");                /* Checks whether the mode of the new record is set. */                var newmode = this.isNewMode();                /* If the value is not set and the mode of the new record is set. */                if (!deadline && newmode) {                    /* Retrieve the [StartDate] column value. */                    var newDate = new Date(this.get("StartDate"));                    newDate.setDate(newDate.getDate() + 10);                    /* Set the [Deadline] column value. */                    this.set("Deadline", newDate);                }            }        }    };});