handlers: /**SCHEMA_HANDLERS*/[    {        request: "usr.SomeCustomRequest",        /* The implementation of the custom request handler. */        handler: async (request, next) => {            /* Retrieve the instance of the HTTP client from "@creatio-devkit/common." */            const handlerChain = sdk.HandlerChainService.instance;            /* Send the base request handler to open the "Some page" page of the "SomePageSchema" schema. */            await handlerChain.process({                type: 'crt.OpenPageRequest',                schemaName: 'SomePageSchema',                $context: request.$context,                scopes: [...request.scopes]            });            /* Call the next handler if it exists and return its result. */            return next?.handle(request);        }    }] /**SCHEMA_HANDLERS*/,