handlers: /**SCHEMA_HANDLERS*/[{    /* The custom implementation of the custom request handler. */    request: "usr.AddRecordRequest",    handler: async (request, next) => {        /* The section list collection that the Freedom UI section page displays. */        const collection = await request.$context.Items;        /* Add a new record of the section list collection. */        await collection.createItem({            /* Set the "Name" field to "John Smith." */            initialModelValues: { Name: 'John Smith'}        });        return next?.handle(request);    }}]/**SCHEMA_HANDLERS*/,