handlers: /**SCHEMA_HANDLERS*/[    {        request: "usr.SomeCustomRequest",        /* Implement the custom request handler. */        handler: async (request, next) => {            /* Send the base request to open the "UsrSomeSelectionWindow" selection window. */            sdk.HandlerChainService.instance.process({                type: 'crt.OpenSelectionWindowRequest',                scopes: [...request.scopes],                $context: request.$context,                /* "Object" type schema whose data is displayed in the selection window. Optional. If you omit the "entitySchemaName" property, Creatio uses the value from the attribute specified in the "itemsAttributeName" property.*/                entitySchemaName: 'SomeSchema',                /* Title of the selection window. */                caption: 'SomeString',                /* Schema code of the selection window. Optional. If you omit the "schemaName" property, Creatio uses the "BaseLookupPageTemplate" schema. */                schemaName: 'UsrSomeSelectionWindow',                /* Attribute that stores data of the selection window title and the selection result in the window. Generally, it is the attribute associated with the "Combobox" control. Optional. If you use some attribute as an "itemAttributeName" property value, Creatio sets the result to the attribute value. */                itemAttributeName: 'SomeAttribute1',                /* Attribute that stores data of the data source. Optional. If you specify the "itemsAttributeName" property, Creatio uses the value to get record. */                itemsAttributeName: 'SomeAttribute2',                /* Callback function that handles the records selected in the selection window. */                afterClosed: async (result) => {                    const canceled = result.canceled;                    if (canceled) {                        /* If the selection window does not include any record selected and a user closes the selection window, execute the following business logic. */                        ...;                    }                    if (!result.canceled) {                        /* If the selection window includes selected records, execute the following business logic. */                        const lookupValues = await result.getLookupValues();                        const value = lookupValues[0];                        if (value) {                            /* If the selection window includes selected records, execute the following business logic. */                            ...;                        }                    }                },                /* Filter data. Optional. If you specify the "filtersConfig" property, Creatio filters data. */                filtersConfig: {                    filterAttributes: [                        {                            name: 'SomeFilter',                            loadOnChange: false                        }                    ],                    attributesConfig: {                        SomeFilter: {                            value: {                                /* Implement the business logic that filters data to display in the selection window. */                                ...,                            }                        }                    }                }            });            /* Call the next handler if it exists and return its result. */            return next?.handle(request);        }    }]/**SCHEMA_HANDLERS*/,