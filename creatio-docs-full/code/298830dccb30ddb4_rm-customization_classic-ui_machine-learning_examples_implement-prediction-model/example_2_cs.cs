// IMPORTANT: When implementing// long-running operations,// it is crucial to enable timely and// responsive cancellation. To achieve// this, ensure that your code// is designed to respond appropriately// to cancellation requests using// the context.CancellationToken// mechanism. For more detailed// information and examples,// please, refer to our documentation./* Get an instance of the "UserConnection." */var userConnection = Get<UserConnection>("UserConnection");/* ID of selected account. */Guid entityId = Get<Guid>("RecordId");/* ID of selected ML model. */var modelId = Get<Guid>("MLModelID");var connectionArg = new ConstructorArgument("userConnection", userConnection);/* Create an instance of the "MLEntityPredictor." */var predictor = ClassFactory.Get<MLEntityPredictor>(connectionArg);/* Call the prediction service. Creatio saves data to the "MLPrediction"database table. If high probability is predicted, Creatio saves datato the required field on the account page. */predictor.PredictEntityValueAndSaveResult(modelId, entityId);return true;