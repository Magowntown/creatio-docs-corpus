define("ActivityPageV2", [], function() {    return {        /* The name of the record page object's schema. */        entitySchemaName: "Activity",        /* Display the field on the record page. */        diff: /**SCHEMA_DIFF*/[            /* The properties to add the custom field to the page. */            {                /* Add the element to the page. */                "operation": "insert",                /* The meta name of the parent container to add the field. */                "parentName": "Header",                /* Add the field to the parent element's collection of elements. */                "propertyName": "items",                /* The meta name of the field to add. */                "name": "UsrMeetingPlace",                /* The properties to pass to the element's constructor. */                "values": {                    /* Bind the field caption to the localizable schema string. */                    "caption": {"bindTo": "Resources.Strings.MeetingPlaceCaption"},                    /* Set up the field layout. */                    "layout": {                        /* The column number. */                        "column": 0,                        /* The column span. */                        "row": 5,                        /* The column span. */                        "colSpan": 12                    }                }            }        ]/**SCHEMA_DIFF*/    };});