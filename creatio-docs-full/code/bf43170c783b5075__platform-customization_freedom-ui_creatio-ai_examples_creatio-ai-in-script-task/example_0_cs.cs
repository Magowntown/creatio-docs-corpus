// IMPORTANT: When implementing// long-running operations,// it is crucial to enable timely and// responsive cancellation. To achieve// this, ensure that your code// is designed to respond appropriately// to cancellation requests using// the context.CancellationToken// mechanism. For more detailed// information and examples,// please, refer to our documentation./* Retrieve the "inputText" parameter of the AI Skill. */var infoToSend = Get<string>("InputTextParameter");/* Create a "CopilotIntentCall" instance. */var request = new CopilotIntentCall {    /* Code of the AI Skill to run. */    IntentName = "UsrSkillContactSummary",    /* Parameters of the AI Skill. */    Parameters = new Dictionary<string, object> {        { "inputText", infoToSend }    }};/* Create an instance of the "CopilotIntentCall" object. */CopilotIntentCallResult result = UserConnection.CopilotEngine.ExecuteIntent(request);/* Status of the Creatio.ai execution. */Set("ExecutionStatus", result.Status.ToString());/* Display the message if the "inputText" parameter of the AI Skill is empty or it has incorrect format. Many messages are separated by semicolon. */if (result.Warnings.Count > 0) {    Set("InputTextParameterErrorMessage", string.Join(";", result.Warnings));}/* Display the error message if error occurs during the AI Skill execution. */if (!result.IsSuccess) {    Set("CreatioAIErrorMessage", result.ErrorMessage);    return true;}/* Outcome of the Creatio.ai work. */Set("CreatioAIResult", result.Content);return true;