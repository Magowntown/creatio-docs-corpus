/* Create an instance of the EntitySchemaQuery class with the Contact root schema. */var esq = this.Ext.create("Terrasoft.EntitySchemaQuery", {    rootSchemaName: "Contact"});/* Add an Account lookup column. Then add the Name column from the Account schema linked to the Account lookup column. Set the alias of the Name column to AccountName. */esq.addColumn("Account.Name", "AccountName");/* Add an Account lookup column. Then add the PrimaryContact lookup column from the Account schema linked to the Account lookup column. Then add the Name column from the Contact schema linked to the PrimaryContact lookup column. Set the alias of the Name column to PrimaryContactName. */esq.addColumn("Account.PrimaryContact.Name", "PrimaryContactName");