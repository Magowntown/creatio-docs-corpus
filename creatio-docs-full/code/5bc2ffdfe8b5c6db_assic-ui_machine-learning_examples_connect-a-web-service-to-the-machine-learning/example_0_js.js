define("UsrMLModelPage", [], function() {    return {        /* The name of the page object schema. */        entitySchemaName: "MLModel",        /* The properties of the page view model. */        properties: {            FakeScoringProblemTypeId: "19fcfff1-98b9-4933-8f26-457ca45c35ed"        },        /* The methods of the page view model. */        methods: {            getIsScoring: function() {                const parentResult = this.callParent(arguments);                return parentResult || this.getLookupValue("MLProblemType") === this.FakeScoringProblemTypeId;            }        }    };});