/* Create and add multiple users. */foreach (var employeeData in data) {    var userId = CreateUser(employeeData);    userIds.Add(userId);}/* Recalculate and redistribute licenses for all active users. A single call is more efficient than redistributing licenses for each user individually. */var service = ClassFactory.Get<ILicenseRedistributionService>();service.RedistributeLicences(redistributeManuallyAssignedLicences: false);